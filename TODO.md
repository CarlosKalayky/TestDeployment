okey, this version is less updated, but works fine with the mongodb servers, letting us see the json files with the data from database.
Stil, lets look for a way to make login work properly and then with this be able to again add blogs and users with their corresponding user.
